dependencies:
  - src: therojam.dockerhost 
    src: git+https://github.com/TheRojam/ansible-dockerbase.git
    name: dockerhost 

galaxy_info:
  author: therojam <therojam@therojam.tech>  
  description: rollout workadventure via its docker_compose_file 

  license: BSD-3-Clause

  min_ansible_version: 2.1

  galaxy_tags: 
    - workadventure
    - docker
    - debian

  dependencies: 
    - docker
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
